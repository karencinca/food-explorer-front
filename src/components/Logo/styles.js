import styled from 'styled-components'

export const Container = styled.div`
    display: flex;
    flex-wrap: nowrap;
    gap: 11px;

    > span {
        font-family: 'Roboto', sans-serif;
        font-style: normal;
        font-weight: 700;
        font-size: 2.5rem;
        line-height: 4.4rem;
    }
`